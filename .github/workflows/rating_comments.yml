name: Rating comments

on:
  workflow_dispatch:

jobs:
  retrain:
    runs-on: ubuntu-latest

    permissions:  # <- NECESSARIO PER IL PUSH
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Login to Hugging Face
      run: |
        huggingface-cli login --token "$HF_TOKEN"
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}

    - name: Rate the comments
      run: python src/predict.py

#    - name: Imposta URL con token
#      run: |
#        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
#
#    - name: Commit e push
#      run: |
#        git config user.name "github-actions"
#        git config user.email "actions@github.com"
#        git add logs/comm_log.csv data/batch_progress.json
#        git commit -m "Aggiornamento file da workflow" || echo "No changes to commit"
#        git push origin HEAD:${{ github.ref_name }}
